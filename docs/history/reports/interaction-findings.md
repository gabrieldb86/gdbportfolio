# Validação da atualização dos serviços

O preview confirma que o hero está usando o projeto `Podcast Varejo na Real` como imagem principal, em vez da foto de Gabriel. A seção de serviços expõe os três itens como elementos `button` acessíveis, cada um com número, título, descrição curta e ícone de mais. O primeiro item foi aberto com sucesso: o conteúdo detalhado e o link contextual aparecem abaixo da descrição, e o ícone muda para o estado de fechamento.
